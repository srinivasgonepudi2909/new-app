server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    # SPA fallback so deep links like /dashboard work on refresh
    try_files $uri /index.html;
  }
}
